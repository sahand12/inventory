/**
 * Created by hamid on 6/20/15.
 */
/*$(function () {
    var ctx = document.getElementById('pieChart').getContext('2d');
    var lineCtx = document.getElementById('lineChart').getContext('2d');


    // last 30 days pie chart
    window.myPie = new Chart(ctx).Pie(costApp.pieData);

    // this year line chart
    console.log(lineCtx);
    console.dir(costApp.lineData);
    window.myLine = new Chart(lineCtx).Line(costApp.lineData, { responsive: true });

});

var totalPieValue = function (pieChartData) {
    var total = 0;
    for (var i = 0, len = pieChartData.length; i < len; i++) {
        total += pieChartData[i].value;
    }

    return total;
};

*/
